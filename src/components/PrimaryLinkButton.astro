---
interface Props {
  href: string;
  target?: string;
  rel?: string;
  className?: string;
  size?: 'sm' | 'md';
}

const {
  href,
  target,
  rel,
  className = '',
  size = 'sm',
} = Astro.props;

const sizeClass = size === 'md' ? 'px-8 py-3 text-base' : 'px-6 py-2.5 text-sm';
---

<a
  href={href}
  target={target}
  rel={rel}
  class:list={[
    'inline-flex w-fit items-center justify-center rounded-full font-semibold text-white transition-all duration-200',
    'bg-brand-extra hover:bg-brand-extraDark active:bg-brand-extraDark',
    sizeClass,
    className,
  ]}
>
  <slot />
</a>
